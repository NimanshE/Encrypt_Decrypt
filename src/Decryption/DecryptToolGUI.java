package Decryption;

import javax.swing.*;

public class DecryptToolGUI extends JFrame {
    public DecryptToolGUI() {
        // Frame setup
        JFrame frame = new JFrame("DecryptTool");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setSize(600, 400);
        // Display the window
        frame.setVisible(true);
    }
}